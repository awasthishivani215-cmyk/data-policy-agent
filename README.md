#data policy agent (LLM based config. made via qwen 0.5 b parameter which is fast ) as it is help in checking policy compliances like gdpr,ccpa, data violation and data retention contain 20 dataset of different company policy ...


analyze complaint feature which include moore than 20 complaints analyze and having different complaints and at a last report has been generate a report on which it shows the gdpr,ccpa compliances and also having section of batch upload

make a virtual environment on terminal - venv\Scripts\activate (for windows)
commands - pip install -r- requirements.text
command - python app.py
flask server will run
also ports 8080 with dashboard url and backend server api like api\health\api for backend python(flask) server integerate dand deployed on railway
